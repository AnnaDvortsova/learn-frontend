// Метод toReversed() возвращает новый массив с элементами в обратном порядке. Первый элемент массива становится последним, а последний — первым.

const array = ['cat', 'dog', 'rabbit'];

const result = array.toReversed();
console.log(result); // ['rabbit', 'dog', 'cat']