// Метод forEach() выполняет указанную функцию один раз для каждого элемента в массиве.

const array1 = ['a', 'b', 'c'];

array1.forEach((element, index) => console.log(`Элемент ${element} с индексом ${index}`));
// Элемент a с индексом 0
// Элемент b с индексом 1
// Элемент c с индексом 2